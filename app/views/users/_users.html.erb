<% if users.any? %>
  <ul>
    <% users.each do |user| %>
      <li>
        <div>
          <div>
            <%= user.name %> <%= link_to 'View profile', user_path(user) %>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
  <%= paginate users %>
<% end %>